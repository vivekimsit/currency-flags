{
  "name": "currency-flags",
  "version": "0.2.0",
  "description": "Flags for currency codes.",
  "files": [
    "dist/currency-flags.css",
    "dist/currency-flags.min.css"
  ],
  "scripts": {
    "start": "npm run build --silent",
    "build": "npm run minify-images && npm run inline-images && npm run change-class-names && npm run minify-css && rm -rf build && echo 'Done.'",
    "minify-images": "imagemin 'src/flags/*' build",
    "inline-images": "imacss 'build/*.png' > 'dist/currency-flags.css'",
	"change-class-names": "sed -i '' 's/imacss/currency-flag/g' 'dist/currency-flags.css'",
    "minify-css": "cleancss -o 'dist/currency-flags.min.css' 'dist/currency-flags.css'"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/transferwise/currency-flags.git"
  },
  "author": "oliverviljamaa",
  "license": "MIT",
  "devDependencies": {
    "clean-css": "^3.4.12",
    "imacss": "^1.0.0",
    "imagemin-cli": "^2.1.0"
  }
}
